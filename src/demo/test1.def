delim       [ \t\n]
ws          {delim}+
letter      [a-b]
digit       [0-9]
id          {letter}({letter}|{digit})*
number      {digit}+(\.{digit}+)?(E[+-]?{digit}+)?

%%

ws          {ws}
id          {id}
number      {number}
